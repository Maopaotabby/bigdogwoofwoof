{
  "type": "module",
  "private": true,
  "description": "Static GitHub Pages site. The package file also defines the Cloudflare Worker AI SDK dependency.",
  "scripts": {
    "start:lighthouse": "node server/lighthouse-server.js"
  },
  "dependencies": {
    "openai": "^6.9.1",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "wrangler": "^4.44.0"
  }
}
