# 关键发现

- 默认 AI 直连无法在服务端统一记录 token、间隔和黑名单，因此已改为默认同源代理；自定义 BYOK 仍保留前端直连。
- 32k+ payload 风险来自未硬截断的历史战斗/日志上下文，现在 prompt builder 对战斗历史最多保留 20 场并压缩字段。
- `project/Helper` 会被当作项目内容处理，已将数值编辑器迁移到同级 `Helper`，避免随项目上传。
- 上传 GUI 的相对路径逻辑历史上只识别 `projet`，已兼容 `project`。
- 本地服务首次启动失败是因为未安装依赖，执行 `npm ci` 后服务端 health 与 AI 日志接口正常。
