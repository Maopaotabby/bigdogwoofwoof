# V3.0 综合优化任务计划

## 目标
完成移动端性能/UI、联机房间布局、Helper 编辑器迁移、AI 日志/限流/防滥用、token 超限控制和上传脚本重启验证。

## 阶段

1. [completed] 溯源与设计确认
   - 已读取前端布局、AI 请求链路、服务端、数值编辑器和上传脚本。
   - 已确认视觉方向为黑白几何印花，辅以少量青粉色线条。
2. [completed] AI 安全与 token 控制
   - 默认 AI 改走服务端代理并记录登录卡 token、请求间隔。
   - 战斗历史超过 20 场时压缩传输，避免 32k+ payload。
   - 重复点击同一 AI 请求在客户端复用 promise，服务端也记录 request key。
   - 每角色卡每日每类 5 次：角色生成、人生经历、战斗总结。
   - 增加登录卡 IP 黑名单与高频/大容量拦截。
3. [completed] 前端移动端与联机 UI
   - 主界面追加黑白印花集覆盖样式。
   - 移动端关闭重动画、阴影和 backdrop-filter。
   - 联机创建房间与加入房间改为并排网格，窄屏回落单列。
4. [completed] 数值编辑器迁移与扩展
   - `project/Helper/card-value-editor` 已迁移到 `V3.0/Helper/card-value-editor`。
   - 编辑器服务改为读取 `V3.0/project` 数据。
   - 界面文案汉化，并新增“添加手札”模板牌功能。
5. [completed] 上传脚本修复与发布验证
   - 上传脚本路径识别支持 `project/projet`。
   - `-RestartOnly` 已验证可重启远程服务。
   - 上传覆盖 root-owned 文件时会回退到 `/tmp` + `sudo install`。
   - 已补充并部署 Nginx `/api/` 代理。
   - 本地静态检查、远端 health、远端 AI 日志接口、每日额度与重复请求均已验证。

## V3.1 本轮补完任务状态

2. [completed] 自定义角色特殊手札编辑与查看
   - 对战设置的登录卡角色管理加入图形化角色编辑器，可编辑角色面板、术式资料和特殊手札数值，并保留角色 JSON 获取/应用接口。
3. [completed] 默认与自定义 AI Key、内置 prompt 逻辑检修
   - 自定义 AI 模式改为仅使用用户提供的 Base URL、Model 和 Key，不再错误走默认模型/代理；角色生成 prompt 增强为严格 JSON 输出和保守字段规则。
4. [completed] 界面冗余收起
   - 自定义导入、特殊手札编辑、手动输入、定义库说明等低频窗口改为默认收起。
5. [completed] 百科全书
   - 自定义手札编写指导与百科归入情报档案，并增加悬浮按钮/悬浮小窗形态。
6. [completed] 转盘 specialHandTags 溢出
   - 修正 specialHandTags 过滤逻辑，避免转盘剧情/自定义角色误带入其他官方术式标签。
7. [completed] 性能与加载
   - 联机开战手札候选生成加入回合级缓存，减少进入战斗时的重复构建。
8. [completed] 单机 CPU 对战 AI 重做
   - CPU 规划加入策略分析、斩杀快速通道、行动序列评分、先增益后攻击偏置和可解释规划日志，保留轻量 Beam Search 思路。
9. [completed] 多人联机 2V2
   - 联机创建/进入流程补齐 2V2 选角组合，开战时自动合成双方队伍角色并导入战斗运行时。
10. [completed] 杂项优化与腾讯端推送
   - 降低体术差距闪避极值；修正投射咒法 24 帧后结算偏低问题；新增“出框时刻”异常状态标签；Cloudflare Pages 已部署并验证标题“怀旧服”与 geo router 生效；腾讯端已部署并通过公网 health 验证；腾讯一键更新包装脚本已修正并复跑通过。
